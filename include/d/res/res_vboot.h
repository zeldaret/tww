#ifndef RES_VBOOT_H
#define RES_VBOOT_H

#include "global.h" // IWYU pragma: keep

enum VBOOT_RES_FILE_ID { // IDs and indexes are synced
    /* BDLM */
    VBOOT_BDL_VBOOT=0x4,
    
    /* BTK */
    VBOOT_BTK_VBOOT=0x7,
};

#endif /* RES_VBOOT_H */
